# 主页面设计建议

## 当前设计方向

本版以 **“网络安全体验区”** 为唯一主标题，将页面由深色控制台调整为明亮、通透的体验入口。视觉重点集中在标题、三张场景卡片和卡片内的动态轨道图形上，避免将用户注意力分散到状态码、英文旁注和低优先级说明中。

| 设计要素 | 当前处理 | 目的 |
|---|---|---|
| 首屏层级 | 大标题与三个并列入口 | 用户进入页面即可理解页面目的并选择场景。 |
| 色彩语言 | 青绿、金色、珊瑚红分别对应下载、邮件与勒索风险 | 不依赖额外说明文字，也能建立场景辨识。 |
| 材质 | 半透明白色面板、柔光边缘与浅色网格 | 提升亮度和科技感，同时维持清晰可读性。 |
| 动态 | 光场漂移、粒子浮动、涟漪扩散、卡片入场与悬浮反馈 | 让页面保持生命力，但不妨碍阅读或点击。 |

## 推荐的后续优化

建议保持“一个核心信息、三个直接行动”的内容密度。首页不宜再增加说明段落、数值面板或状态标签；如果需要展示培训进度，应仅在用户进入具体场景后出现，以避免入口页重新变得拥挤。

建议将动态效果分为三个节奏层级。背景动效应使用缓慢的 8 至 20 秒循环，作为环境氛围；卡片悬浮和按钮反馈宜控制在 180 至 350 毫秒内，确保交互显得顺滑且直接；点击进入场景时可使用一次性的 300 至 500 毫秒过渡，建立“进入演练”的仪式感。页面已经保留减少动态效果偏好支持，后续扩展也应继续遵守这一约束。

| 优先级 | 建议 | 预期效果 |
|---|---|---|
| 高 | 为三个场景补充一致的进入过渡，例如卡片聚焦、淡入与场景背景接管 | 增强从首页到具体演练的连续性。 |
| 高 | 为键盘焦点、悬浮和点击状态提供与场景色一致的反馈 | 提升可访问性和专业完成度。 |
| 中 | 在大屏下保持三卡并列，在小屏下采用横向滑动卡片或紧凑纵向列表 | 让移动端保留场景入口的优先级。 |
| 中 | 如有品牌规范，可将现有图标替换为统一的扁平化安全图形系统 | 强化品牌识别并提高视觉一致性。 |
| 低 | 仅在后续扩展更多场景时加入轻量筛选或推荐逻辑 | 避免在当前三场景规模下制造多余操作。 |

> 设计原则是让用户先感受到页面的品质，再在极短时间内理解下一步操作。对于当前三场景结构，克制的信息密度和高质量交互反馈比增加更多模块更重要。
